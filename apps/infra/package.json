{
  "name": "@raffleroyale/infra",
  "version": "0.1.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "tsx --test test/*.test.ts",
    "synth": "cdk synth --quiet",
    "deploy": "cdk deploy"
  },
  "dependencies": {
    "@raffleroyale/cdk-constructs": "0.1.0",
    "aws-cdk-lib": "^2.262.1",
    "constructs": "^10.7.1"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "aws-cdk": "^2.1133.0",
    "tsx": "^4.20.5",
    "typescript": "^5.7.3"
  }
}
