/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  UserFindActivityQueryResponse,
  UserFindActivityPathParams,
} from "../models/UserFindActivity.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getUserFindActivityUrl(id: UserFindActivityPathParams["id"]) {
  const res = { method: "GET", url: `/user/${id}/activity` as const };
  return res;
}

/**
 * {@link /user/:id/activity}
 */
export async function userFindActivity(
  id: UserFindActivityPathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    UserFindActivityQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getUserFindActivityUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
