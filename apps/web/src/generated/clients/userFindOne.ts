/**
 * Generated by Kubb (https://kubb.dev/).
 * Do not edit manually.
 */

import fetch from "@kubb/plugin-client/clients/axios";
import type {
  UserFindOneQueryResponse,
  UserFindOnePathParams,
} from "../models/UserFindOne.ts";
import type {
  Client,
  RequestConfig,
  ResponseErrorConfig,
} from "@kubb/plugin-client/clients/axios";

function getUserFindOneUrl(id: UserFindOnePathParams["id"]) {
  const res = { method: "GET", url: `/user/${id}` as const };
  return res;
}

/**
 * {@link /user/:id}
 */
export async function userFindOne(
  id: UserFindOnePathParams["id"],
  config: Partial<RequestConfig> & { client?: Client } = {},
) {
  const { client: request = fetch, ...requestConfig } = config;

  const res = await request<
    UserFindOneQueryResponse,
    ResponseErrorConfig<Error>,
    unknown
  >({
    method: "GET",
    url: getUserFindOneUrl(id).url.toString(),
    ...requestConfig,
  });
  return res.data;
}
