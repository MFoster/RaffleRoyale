# Web App (Prototype MVP)

This frontend provides a prototype flow for:

1. Sign up
2. Log in
3. Create/list raffles
4. Purchase tickets

## Local run

From repository root:

```bash
npm run dev:web
```

Set `NEXT_PUBLIC_API_URL` if API is not on `http://localhost:3001`.
